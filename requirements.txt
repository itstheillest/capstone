# --- Web framework ---
Django==6.0.7
djangorestframework==3.15.2
django-cors-headers
drf-spectacular
Pillow==10.4.0
# psycopg2-binary==2.9.9      # Postgres driver — uncomment only when you switch to Postgres (see note below)
python-dotenv==1.0.1
psycopg2-binary

# --- Async task queue (recommended: ML inference is too slow for a request/response cycle) ---
celery==5.4.0
redis==5.0.8

# --- Computer vision / forensics ---
opencv-python-headless
PyExifTool==0.5.6             # Python wrapper; also requires the `exiftool` CLI binary on the host
face_recognition==1.3.0       # requires dlib + cmake to build; see note below
deepface==0.0.93

# --- Deep learning ---
torch==2.4.1                  # pick torch OR tensorflow as your primary framework, not both, to avoid CUDA conflicts
torchvision==0.19.1
# tensorflow==2.17.0           # uncomment if you standardize on TF instead of torch
transformers==4.44.2          # for the lightweight BERT NLP model
ultralytics==8.2.103          # YOLOv8

# --- NOTE on Detectron2 ---
# Detectron2 is NOT on PyPI. Install it separately from source, matched to
# your torch/CUDA version:
#   pip install 'git+https://github.com/facebookresearch/detectron2.git'
# Only do this if YOLOv8 alone isn't sufficient for your region-proposal needs.

# --- Data handling ---
pandas==2.2.2
numpy==1.26.4

# --- External APIs (Google Reverse Image / Vision, Fact Check Tools) ---
google-api-python-client==2.145.0
requests==2.32.3

# --- Production server ---
gunicorn==23.0.0

google-cloud-vision 
# --- NOTE on psycopg2-binary ---
# Commented out above because pinned old versions sometimes lack a prebuilt
# wheel for newer Python/OS combos (e.g. Python 3.13 on Windows), which makes
# pip try to build from source and fail with "pg_config executable not found".
# You don't need this at all while using SQLite (the default). When you're
# ready for Postgres, just run:
#   pip install psycopg2-binary
# (no version pin) so pip grabs whichever build has a wheel for your Python.

# --- NOTE on face_recognition/dlib ---
# On Ubuntu you'll need build tools first:
#   sudo apt-get install -y cmake build-essential libopenblas-dev liblapack-dev
# On Windows, use a prebuilt dlib wheel instead of building from source.

